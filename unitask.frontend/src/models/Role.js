class Role {
    constructor(roleId, name, description, users, permissions) {
        this.roleId = roleId;
        this.name = name;
        this.description = description;
    }
}

export default Role;